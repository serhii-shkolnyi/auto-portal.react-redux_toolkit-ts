export enum EAccountType {
    BASIC = "basic",
    PREMIUM = "premium",
}

export enum EAccountStatus {
    ACTIVE = "active",
    INACTIVE = "inactive",
    BLOCKED = "blocked",
    DELETED = "deleted",
}